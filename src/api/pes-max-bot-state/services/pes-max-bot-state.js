'use strict';

/**
 * pes-max-bot-state service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pes-max-bot-state.pes-max-bot-state');
